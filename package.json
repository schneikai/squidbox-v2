{
  "name": "squidbox-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": "20.17.0"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  }
}

